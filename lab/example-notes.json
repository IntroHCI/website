[
  [
    {
      "type": "video",
      "title": "This Replaces 'Video' (works for all types)",
      "src": ""
    },
    {
      "type": "links",
      "list": [ "<a href='www.example.com'>Example Link</a>" ]
    },
    {
      "type": "code",
      "comment": """
        To encode any HTML correctly, go into the Python command line and do:
        import cgi
        code = [your big long string here, multiline using triple quotes]
        unescaped = cgi.escape(code, quote=True)
      """
      "text": "[example] cd something_here"
    }
  ],
  [
    {
      "type": "debug",
      "list": [{
        "problem":
        "I don't understand how to generate a lab from the keynote.",
        "fix":
        "Read the 'Readme.md' file and look at the 'example-notes.json'"
      }]
    }
  ]
]